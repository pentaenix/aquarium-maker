{
  "name": "aquarium-maker",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/sync-build.mjs && node scripts/make-standalone.mjs",
    "preview": "vite preview",
    "check": "tsc --noEmit && vite build"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3"
  }
}
